// configures the settings for the user
/* 
Sets the secretKey for jwt, for instance: no, we will only have one secret key.
*/

import { randomBytes } from "crypto";

class Userbase {

}

console.log(randomBytes(839))